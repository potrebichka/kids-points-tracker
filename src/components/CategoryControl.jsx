import React from 'react';

const CategoryControl = (props) => {
    return (
        <div>
            CategoryControl
            {props.name}
            {props.items}
        </div>);
}

export default CategoryControl;